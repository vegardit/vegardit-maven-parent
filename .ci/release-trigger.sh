# this file is evaluated by .ci/build.sh

POM_CURRENT_VERSION="6.1.9-SNAPSHOT" # perform release if pom.xml matches this version
POM_RELEASE_VERSION="6.1.9" # next release version

DRY_RUN=false # is dry run?
SKIP_TESTS=true # skip tests during release build?
